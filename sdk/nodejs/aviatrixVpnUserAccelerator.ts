// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The **aviatrix_vpn_user_accelerator** resource manages the [Aviatrix VPN User Accelerator](https://docs.aviatrix.com/HowTos/user_accelerator.html).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aviatrix from "@pulumi/aviatrix";
 *
 * // Create an Aviatrix Vpn User Accelerator
 * const testVpcAccelerator = new aviatrix.AviatrixVpnUserAccelerator("test_vpc_accelerator", {
 *     elbName: "Aviatrix-vpc-abcd2134",
 * });
 * ```
 *
 * ## Import
 *
 * **vpn_user_accelerator** can be imported using the `elb_name`, e.g.
 *
 * ```sh
 *  $ pulumi import aviatrix:index/aviatrixVpnUserAccelerator:AviatrixVpnUserAccelerator test Aviatrix-vpc-abcd1234
 * ```
 */
export class AviatrixVpnUserAccelerator extends pulumi.CustomResource {
    /**
     * Get an existing AviatrixVpnUserAccelerator resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AviatrixVpnUserAcceleratorState, opts?: pulumi.CustomResourceOptions): AviatrixVpnUserAccelerator {
        return new AviatrixVpnUserAccelerator(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'aviatrix:index/aviatrixVpnUserAccelerator:AviatrixVpnUserAccelerator';

    /**
     * Returns true if the given object is an instance of AviatrixVpnUserAccelerator.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AviatrixVpnUserAccelerator {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AviatrixVpnUserAccelerator.__pulumiType;
    }

    /**
     * Name of ELB to be added to VPN User Accelerator. Example: "Aviatrix-vpc-abcd2134".
     */
    public readonly elbName!: pulumi.Output<string>;

    /**
     * Create a AviatrixVpnUserAccelerator resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AviatrixVpnUserAcceleratorArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AviatrixVpnUserAcceleratorArgs | AviatrixVpnUserAcceleratorState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AviatrixVpnUserAcceleratorState | undefined;
            resourceInputs["elbName"] = state ? state.elbName : undefined;
        } else {
            const args = argsOrState as AviatrixVpnUserAcceleratorArgs | undefined;
            if ((!args || args.elbName === undefined) && !opts.urn) {
                throw new Error("Missing required property 'elbName'");
            }
            resourceInputs["elbName"] = args ? args.elbName : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AviatrixVpnUserAccelerator.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AviatrixVpnUserAccelerator resources.
 */
export interface AviatrixVpnUserAcceleratorState {
    /**
     * Name of ELB to be added to VPN User Accelerator. Example: "Aviatrix-vpc-abcd2134".
     */
    elbName?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a AviatrixVpnUserAccelerator resource.
 */
export interface AviatrixVpnUserAcceleratorArgs {
    /**
     * Name of ELB to be added to VPN User Accelerator. Example: "Aviatrix-vpc-abcd2134".
     */
    elbName: pulumi.Input<string>;
}
